########################
Computer king constraint
########################
correctFile = ($file >= #FileC) && (file <= #FileF);
correctRank = ($rank >= #Rank3) && ($rank <= #Rank6);

##################
Other pieces count
##################
Q 1